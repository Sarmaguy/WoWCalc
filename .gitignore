# ---------------------------------------------------------------------------
# Build output
# ---------------------------------------------------------------------------
# PyInstaller work directory — regenerated on every build.
build/

# Compiled executables. The build is reproducible from wow_calc.spec, so the
# ~10 MB binary does not belong in git history.
*.exe

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ---------------------------------------------------------------------------
# Runtime data
# ---------------------------------------------------------------------------
# Recorded Auction House price snapshots. Written next to the app on every
# paste, so it churns constantly and is specific to one machine/realm.
# Delete this line if you ever want the price log under version control.
history.json

# ---------------------------------------------------------------------------
# Editor / OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini
